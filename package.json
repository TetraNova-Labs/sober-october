{
  "name": "sober-october",
  "version": "1.0.0",
  "description": "",
  "lint-staged": {
    "client/**/*.{js,ts,jsx,tsx}": "bash -c 'cd client && make lint-fix || exit 1'",
    "api/**/*.{js,ts}": "bash -c 'cd api && make lint-fix || exit 1'"
  },
  "scripts": {
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2"
  }
}
